Tokio Server with Redis cache and Mongo persistence using a custom varint + compression protocol.

Build:
- rustc 1.70+ - stable
- MongoDB and Redis running locally

Run:
- cargo run --package tokio_server
